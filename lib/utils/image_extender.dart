/*
import 'package:image/image.dart';

class ImageExtender extends Image {

  ImageExtender(int width, int height) : super(width, height);

  ImageExtender.decodeImage(List<int> data): super.from(decodeImage(data)!);

  rotate(num angle)
  {
    this.super = copyRotate(super, angle);
  }
}*/
